apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-practice.fullname" . }}
  labels:
    {{- include "devops-practice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "devops-practice.selectorLabels" . | nindent 4 }}
